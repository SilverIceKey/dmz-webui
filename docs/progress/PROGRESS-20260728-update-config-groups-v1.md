# 部署更新配置分组进度

## 当前状态

- 状态：根因修复与本地脚本回放完成，等待目标服务器交互验收。
- 当前结论：全局“是否复用”已移除，三个配置分组分别确认且不会相互跳过。

## 最近关键结论

- 公网与 Caddy、页面标题、备案配置分别询问是否修改。
- 任一分组选择不修改时保留原值，并继续询问后续分组。
- 只修改标题的回放确认域名、443 端口、TLS 模式和备案号不变。
- 修改 Caddy 分组时，当前模式决定默认选项；标准 443 配置直接回车仍保持
  标准 443，不会回退为 8443。
- ACME 邮箱直接回车保留当前值，输入 `-` 清空。
- 无历史配置时仍按顺序收集全部配置。
- 后端全量 54 项测试通过，Shell 语法和差异检查通过。

## 下一步

- 在目标服务器执行最新 `update.sh`，依次验证三个确认点。
- 只修改标题并核对 `install.conf`、systemd override 和页面结果。
- 再次执行更新并全部选择不修改，确认配置文件内容保持一致。

## 阻塞项

- 当前开发环境不是目标服务器，尚未执行真实 TTY 交互和 systemd 重启。

## 未证实风险

- 如果使用已经启动的旧版 `update.sh` 自更新，当前进程仍按旧逻辑运行；代码
  更新完成后需重新运行最新版脚本才能看到分组提示。
